import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { MigrationSummaryRequest } from '../request/MigrationSummaryRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class MigrationSummaryToolImpl extends BaseTool<MigrationSummaryRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "(For migration only) Generates a summary for the migration session, including details about the migration scenario, task name, modified files, and the final status of build and test fix operations. This tool helps you document the migration process and its outcomes.";
    execute(request: MigrationSummaryRequest, context: McpContext): Promise<ToolResponse>;
}
//# sourceMappingURL=MigrationSummaryToolImpl.d.ts.map