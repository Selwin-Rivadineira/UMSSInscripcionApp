import { MCPLogger } from '../logger';
import { KB } from '../types';
import { Document, DocumentLoaderLike } from './loaders';
import { IKBLoader, IKBStoreChangeCallbacks } from './store';
export declare class MarkdownToKb implements IKBLoader {
    private documentLoader;
    private logger;
    constructor(documentLoader: DocumentLoaderLike, logger: MCPLogger);
    load(): Promise<KB[]>;
    markdownConverter(doc: Document): {
        ok: boolean;
        converted: KB;
    };
    onChange(callbacks: IKBStoreChangeCallbacks): void;
}
//# sourceMappingURL=markdown_to_kb.d.ts.map