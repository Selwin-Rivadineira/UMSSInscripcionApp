"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.tryResolvePath = tryResolvePath;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
function tryResolvePath(root, partialPath) {
    if (path.isAbsolute(partialPath)) {
        if (fs.existsSync(partialPath)) {
            return { success: true, fullPath: partialPath };
        }
        return { success: false, fullPath: undefined };
    }
    if (!root.trim() || !partialPath.trim()) {
        return { success: false, fullPath: undefined };
    }
    // Normalize the path separator
    partialPath = path.normalize(partialPath);
    const pathParts = partialPath.split(path.sep);
    for (const file of enumerateFiles(root, pathParts[pathParts.length - 1])) {
        if (file.endsWith(partialPath)) {
            return { success: true, fullPath: file };
        }
    }
    return { success: false, fullPath: undefined };
}
function* enumerateFiles(dir, fileName) {
    const list = fs.readdirSync(dir);
    for (const file of list) {
        const resolvedPath = path.resolve(dir, file);
        const stat = fs.statSync(resolvedPath);
        if (stat && stat.isDirectory()) {
            yield* enumerateFiles(resolvedPath, fileName);
        }
        else if (resolvedPath.endsWith(fileName)) {
            yield resolvedPath;
        }
    }
}
//# sourceMappingURL=fileSystem.js.map