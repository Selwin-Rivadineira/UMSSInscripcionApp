import { ILogger } from './common/types';
import { Environment } from './context/Environment';
import { IJavaProjDeps } from './dependency/JavaDependency';
import { setCmdInfo } from './IBuildTool';
import { JavaBuildTool } from './JavaBuildTool';
import { IBuildResult, TTestResult } from './test/types';
import { gradleCmd } from './utils/gradle';
export declare class GradleBuildTool extends JavaBuildTool {
    private buildCmd;
    protected env: Environment;
    private cmdInfo?;
    private logger?;
    private signal?;
    constructor(buildCmd: gradleCmd | undefined, repoPath: string, env?: Environment, cmdInfo?: setCmdInfo, logger?: ILogger | undefined, signal?: AbortSignal | undefined);
    getDirectDependencies(): Promise<{
        parsed: IJavaProjDeps;
        raw: string;
    }>;
    compile(): Promise<IBuildResult>;
    test(): Promise<TTestResult>;
}
//# sourceMappingURL=GradleBuildTool.d.ts.map