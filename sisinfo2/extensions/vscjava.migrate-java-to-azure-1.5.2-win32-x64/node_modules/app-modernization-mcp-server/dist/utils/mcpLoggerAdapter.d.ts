import { ILogger, ILogOpts, LogLevel } from '../javabuild/common/types';
import { MCPLogger } from '../logger';
export declare class McpLoggerAdapter implements ILogger {
    private readonly mcpLogger;
    constructor(mcpLogger: MCPLogger);
    error(message: string, opts?: ILogOpts): void;
    info(message: string, opts?: ILogOpts): void;
    log(message: string, level: LogLevel, opts?: ILogOpts): void;
    warn(message: string, opts?: ILogOpts): void;
}
//# sourceMappingURL=mcpLoggerAdapter.d.ts.map