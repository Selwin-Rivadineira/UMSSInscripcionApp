import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { CompletenessValidationRequest } from '../request/CompletenessValidationRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class CompletenessValidationToolImpl extends BaseTool<CompletenessValidationRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "This tool validates migration completeness by systematically discovering old technology references that should have been migrated but were left unchanged. It generates detailed validation guidelines to identify missing removals and incomplete transformations across all files in the workspace.";
    execute(request: CompletenessValidationRequest, context: McpContext): Promise<ToolResponse>;
}
//# sourceMappingURL=CompletenessValidationToolImpl.d.ts.map