import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { VersionControlRequest } from '../request/VersionControlRequest';
import { BaseTool, ToolResponse } from '../Tool';
/**
 * A tool to perform version control operations
 * Using GitVersionControl as the default implementation
 */
export declare class VersionControlToolImpl extends BaseTool<VersionControlRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "This tool provides version control operations for the migration process. It handles operations like checking repository status, creating branches, committing changes, stashing changes, and discarding changes.";
    private versionControl;
    constructor();
    execute(request: VersionControlRequest, context: McpContext): Promise<ToolResponse>;
}
//# sourceMappingURL=VersionControlToolImpl.d.ts.map