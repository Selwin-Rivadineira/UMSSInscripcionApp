import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { SearchKbRequest } from '../request/SearchKnowledgeBaseRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class SearchKnowledgeBaseToolImpl extends BaseTool<SearchKbRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "Searches the application modernization knowledge base using a query string. Returns relevant articles, documentation, and best practices. Use this tool to find information about app modernization strategies, migration paths, and common patterns.";
    execute(request: SearchKbRequest, context: McpContext): Promise<ToolResponse>;
    private formatKBSearchResults;
    private formatEachKb;
}
//# sourceMappingURL=SearchKnowledgeBaseToolImpl.d.ts.map