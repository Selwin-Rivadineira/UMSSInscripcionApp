import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { GetKbRequest } from '../request/GetKnowledgeBaseRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class GetKnowledgeBaseToolImpl extends BaseTool<GetKbRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "Retrieves the complete content of a specific knowledge base article using its ID. Use this tool when you have a document ID and need to access its full content for detailed information about app modernization techniques.";
    private getKb;
    execute(request: GetKbRequest, context: McpContext): Promise<ToolResponse>;
    private formatEachKb;
}
//# sourceMappingURL=GetKnowledgeBaseToolImpl.d.ts.map