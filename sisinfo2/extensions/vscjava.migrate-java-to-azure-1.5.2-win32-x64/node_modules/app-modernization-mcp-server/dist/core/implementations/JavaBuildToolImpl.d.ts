import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { JavaBuildToolRequest } from '../request/JavaBuildToolRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class JavaBuildToolImpl extends BaseTool<JavaBuildToolRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "(For migration only) This tool uses Maven or Gradle to build your project and report any detected errors after migration. You should use this tool to verify after making code changes, and fix any errors found.";
    execute(request: JavaBuildToolRequest, context: McpContext): Promise<ToolResponse>;
}
//# sourceMappingURL=JavaBuildToolImpl.d.ts.map