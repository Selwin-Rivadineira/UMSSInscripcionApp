import { McpContext } from '../../types';
import { SearchFileRequest } from '../request/SearchFileRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare class SearchFileToolImpl extends BaseTool<SearchFileRequest, ToolResponse> {
    name: string;
    toolReferenceName: string;
    displayName: string;
    description: "Searches content from files in the project workspace by query and file patterns. Returns a list of files that match the search query. Use this tool to find specific code files or resources in your project based on patterns.";
    private search;
    execute(request: SearchFileRequest, context: McpContext): Promise<ToolResponse>;
    private stringToRegex;
}
//# sourceMappingURL=SearchFileToolImpl.d.ts.map