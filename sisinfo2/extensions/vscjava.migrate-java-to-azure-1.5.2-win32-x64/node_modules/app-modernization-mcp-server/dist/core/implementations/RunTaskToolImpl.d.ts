import { McpToolNames } from '../../tools/ToolNames';
import { McpContext } from '../../types';
import { RunTaskRequest } from '../request/RunTaskRequest';
import { BaseTool, ToolResponse } from '../Tool';
export declare const planFileName = "plan.md";
export declare class RunTaskToolImpl extends BaseTool<RunTaskRequest, ToolResponse> {
    name: McpToolNames;
    toolReferenceName: McpToolNames;
    displayName: string;
    description: "This tool will start a new migration session to run task based on given scenario or knowledge base id. This tool will provide a comprehensive migration plan, including steps to build the project, run tests, fix test failures, and more. It is designed to automate the migration process for the applications.";
    execute(request: RunTaskRequest, context: McpContext): Promise<ToolResponse>;
    private configureGitUserForBenchmark;
    private setupOutDirectoryGitignore;
}
//# sourceMappingURL=RunTaskToolImpl.d.ts.map