import { MCPLogger } from '../logger';
import { Settings } from '../settings';
import { EmbeddingsProvider, Reranker } from './store';
export declare class XenovaInferenceService implements EmbeddingsProvider, Reranker {
    private settings;
    private logger;
    private embedder;
    private mutex;
    constructor(settings: Settings, logger: MCPLogger);
    private initEmbed;
    computeEmbeddings(text: string): Promise<number[]>;
    rerank(query: string, candidates: string[]): Promise<{
        index: number;
        score: number;
    }[]>;
}
//# sourceMappingURL=inference.d.ts.map