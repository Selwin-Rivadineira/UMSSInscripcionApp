import { IJavaProjDeps } from './dependency/JavaDependency';
import { IDependency } from './dependency/types';
import { IBuildResult, TTestResult } from './test/types';
export interface IDependencyRetriever {
    getDirectDependencies(): Promise<{
        parsed: IJavaProjDeps;
        raw: string;
    }>;
    getDependencies(): Promise<IDependency[]>;
}
export interface IBuildTool extends IDependencyRetriever {
    compile(): Promise<IBuildResult>;
    test(): Promise<TTestResult>;
}
export type setCmdInfo = ((cmd: string, args: string[]) => void) | undefined;
//# sourceMappingURL=IBuildTool.d.ts.map