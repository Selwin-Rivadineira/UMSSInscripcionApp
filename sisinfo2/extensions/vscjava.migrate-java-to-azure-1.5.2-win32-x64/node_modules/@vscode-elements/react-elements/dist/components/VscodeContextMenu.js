import React from "react";
import { createComponent } from "@lit/react";
import { VscodeContextMenu as WC, } from "@vscode-elements/elements/dist/vscode-context-menu/vscode-context-menu.js";
const VscodeContextMenu = createComponent({
    tagName: "vscode-context-menu",
    elementClass: WC,
    react: React,
    displayName: "VscodeContextMenu",
    events: {
        onVscContextMenuSelect: "vsc-context-menu-select",
    },
});
export default VscodeContextMenu;
